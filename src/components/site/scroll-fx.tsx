/**
 * @deprecated GSAP-driven scroll effects have been replaced by Framer Motion
 * primitives in `./motion.tsx` (`FadeIn`, `Stagger`, `Parallax`). This file is
 * kept as an inert no-op so it can be safely removed by the project owner.
 */
export function ScrollFX() {
  return null;
}
